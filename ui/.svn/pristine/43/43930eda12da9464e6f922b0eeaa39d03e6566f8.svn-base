<select ng-attr-id="{{ id }}" ng-attr-name="{{ name }}" class="{{ class }}" ng-disabled="readonly" ng-model="ngmodelvar">
    <option ng-repeat="i in ctrl.academicYears" ng-selected="ngmodelvar == i.id" ng-value="{{ i.id }}" value="{{ i.id }}">{{ i.code }} - {{ i.description }}</option>
</select>