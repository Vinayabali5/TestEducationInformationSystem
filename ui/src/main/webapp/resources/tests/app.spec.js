/*
describe('cid', function() {

    beforeEach(inject(function($injector) {
        $httpBackend = $injector.get('$httpBackend');
        $httpBackend.when('GET','http://localhost:9001/api/test').respond([{name:'test'}]);



    }));

    it('should check something', function() {
        $httpBackend.expectGET('http://localhost:9001/api/test');

        someService.query();
    });

});
*/
